import {
  StyleSheet, Platform,
} from 'react-native';

export default StyleSheet.create({
  keyboard: {
    flex: 1,
  },
  scrollContentContainer: {
    flexGrow: 1,
  },
  containerView: {
    flex: 1,
  },
});